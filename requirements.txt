# Core dependencies for TTT4Rec
# Based on actual imports in main.py and ttt.py
# 
# Analysis:
# - main.py imports: torch, recbole
# - ttt.py imports: torch, transformers, causal-conv1d (optional)

# PyTorch - required by both main.py and ttt.py
# README specifies PyTorch 1.12+, but using 2.4.1 for compatibility with transformers
torch>=1.12.0

# NumPy - required by PyTorch and RecBole
# RecBole is not compatible with NumPy 2.0 (uses deprecated np.float)
numpy<2.0.0

# RecBole - required by main.py for recommendation system framework
# Provides: Config, Trainer, create_dataset, data_preparation, SequentialRecommender, etc.
recbole>=1.2.0

# Transformers - required by ttt.py
# Provides: PretrainedConfig, PreTrainedModel, ACT2FN, ModelOutput, etc.
transformers>=4.44.2
